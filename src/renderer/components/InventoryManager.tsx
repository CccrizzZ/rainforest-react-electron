// import { useState, useEffect } from 'react';

const InventoryManager = () => {
  // const [inventoryArr, setInventoryArr] = useState([]);
  // useEffect(() => {
  //   setInventoryArr([]);
  // }, []);

  return <h1>InventoryManager</h1>;
};

export default InventoryManager;
